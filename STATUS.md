# حالة التنفيذ — المرحلة 1

> يُحدَّث مع كل جلسة عمل. الترتيب المرجعي بـ[ROADMAP.md](ROADMAP.md)، القرارات التقنية بـ[TECH.md](TECH.md).

## ✅ تم التحقق فعليًا — الموقع يشتغل

بتاريخ 2026-08-14 اشتغل الموقع لأول مرة على جهاز فيه إنترنت بدون قيود، وانفحص فعليًا
(build كامل، `npm run dev`، وسكرين شات حقيقي عبر Playwright للرئيسية وصفحة فئة وتسجيل
الدخول، زائد اختبار بحث تفاعلي). **صفر أخطاء TypeScript، صفر أخطاء كونسول.**

## 🚀 النشر (2026-08-15)

الموقع منشور فعليًا على Vercel: **https://2026-opal-nine.vercel.app**

- ربط GitHub↔Vercel: كل push لـ`main` يشغّل نشر جديد تلقائيًا
- أول نشر فشل بخطأ 500 (Application error) — السبب: متغيرات البيئة اللي اتضافت من لوحة
  Vercel وقت الاستيراد الأولي **ما اتحفظت فعليًا** (`vercel env ls production` رجعت "No
  Environment Variables found" رغم إنها كانت تظهر بالفورم). انحلّت بإضافتها من جديد عبر
  Vercel CLI مباشرة (`vercel env add ... production`)
- متغيرات البيئة المضبوطة بالإنتاج حاليًا: `NEXT_PUBLIC_SUPABASE_URL`،
  `NEXT_PUBLIC_SUPABASE_ANON_KEY`، `NEXT_PUBLIC_SITE_URL`، `INTERACTION_HASH_SECRET`
  (قيمة منفصلة عن المحلي، ولّدت خصيصًا للإنتاج)
- ⚠️ **`SUPABASE_SERVICE_ROLE_KEY` لسا فاضي بالإنتاج** — لازم تجيبه من Supabase Dashboard
  (Project Settings → API → service_role) وتضيفه، وإلا لوحة الأدمن ونظام الدفع (لما يفعّل)
  ما راح يشتغلوا بالموقع المنشور
- `TAP_SECRET_KEY`/`TAP_WEBHOOK_SECRET` متروكين فاضيين عمدًا — مؤجّلين حسب قرارك

### ✅ تم التحقق فعليًا (2026-08-15): الموقع المنشور شغّال
`curl` مباشر لـ`https://2026-opal-nine.vercel.app/` رجع 200 والمحتوى الفعلي (الفئات الخمس،
اسم "سوق الزلفي") يظهر صح من Supabase.

### الطريق كان أطول من المتوقع — توثيق كامل للمشاكل حل بحل
1. **متغيرات البيئة ما اتحفظت** من فورم استيراد المشروع الأول بلوحة Vercel — `vercel env ls
   production` رجعت فاضية رغم إنها ظهرت بالفورم. انحلّت بإضافتها من جديد عبر CLI.
2. **حظر نشر بسبب هوية Git (`deployment_blocked`)**: رسالة Vercel وضحت السبب حرفيًا —
   "The deployment was blocked because the commit author did not have contributing access
   to the project on Vercel. The Hobby Plan does not support collaboration for private
   repositories." — صار حتى مع نشر مباشر عبر CLI (مو بس push)، لأن Vercel يربط هوية مؤلف
   آخر كوميت Git بصلاحيات المستودع بغض النظر عن طريقة النشر.
3. **الحل المجاني المطبَّق**: حوّلنا ريبو GitHub من خاص (private) لعام (public) —
   `gh repo edit ... --visibility public`. هذا يزيل قيد "collaboration for private
   repositories" بدون أي ترقية مدفوعة (يطابق مبدأ التكلفة بـ[TECH.md](TECH.md)). قبل
   التحويل، فحصنا **كامل تاريخ الكوميتات** (أسماء ملفات + محتوى كل كوميت بحثًا عن أنماط
   مفاتيح JWT/سرية) وتأكدنا صفر أسرار مكشوفة — الشي الوحيد المرفوع أصلًا هو
   `NEXT_PUBLIC_SUPABASE_ANON_KEY` وهو مفتاح "publishable" مصمم يكون عام أصلًا.
4. بعد التحويل، `vercel deploy --prod` نجح مباشرة (`readyState: READY`, 32 ثانية بناء) وتولّى
   لقب الإنتاج (`Aliased → 2026-opal-nine.vercel.app`) تلقائيًا.

**ملاحظة للمستقبل:** بما إن الريبو صار عام، push مستقبلي لـ`main` يفترض يشغّل نشر Vercel
تلقائي بدون حظر (نفس آلية GitHub↔Vercel، بس بدون قيد الخصوصية الآن). **تأكدنا فعليًا:**
push تجريبي بعد التحويل نجح تلقائيًا (42 ثانية بناء، Ready) بدون أي تدخل يدوي.

## 🟡 المرحلة 2 — أول ميزة: تصفح حسب الحي (2026-08-16)

أول ميزة من [ROADMAP.md](ROADMAP.md) مرحلة 2 (أعلى إجماع بين 5 مصادر مستقلة). البنية التحتية
(جدول `neighborhoods` + `listings.neighborhood_id`) كانت جاهزة من المرحلة 1، فكان الشغل كله
بالطبقة التطبيقية:

- `/neighborhood/[slug]` — صفحة عامة جديدة تعرض كل الإعلانات المنشورة بحي معيّن (بكل الفئات
  مع بعض، مو مفصولة)، بنفس نمط `/category/[slug]`
- الرئيسية: قائمة منسدلة "تصفح حسب الحي" (26 حي — قائمة منسدلة أنسب من شبكة أزرار مثل الفئات
  الخمس) عبر `components/neighborhood-select.tsx` جديد
- البائع يقدر يحدد حي إعلانه (اختياري) وقت الإضافة/التعديل — حقل جديد بالفورمين
- صفحة تفاصيل الإعلان تعرض رابط الحي (تنقّل متبادل بين صفحة الإعلان وصفحة الحي)
- `sitemap.xml` يشمل الـ26 رابط حي تلقائيًا

**اختُبر فعليًا محليًا:** `tsc`/`build` نظيفين، تشغيل `npm run dev` وفحص HTTP مباشر —
الرئيسية تعرض أسماء الأحياء الحقيقية بالقائمة المنسدلة، `/neighborhood/as-sadiq` يرجع 200
بعنوان صحيح، `/neighborhood/نص-غلط` يرجع 404، والـsitemap يحتوي بالضبط 26 رابط حي.

⏳ **ما فيه إعلانات حقيقية بعد** بأي حي (متوقع — لسا بأول الإطلاق) — الاختبار الفعلي
بإعلان منشور حقيقي يصير أول ما يسجل أول بائع.

### بيانات تجريبية للعرض (⚠️ تحتاج تطبيق يدوي — migration 17)
بائع تجريبي واحد (محل) + أسرة منتجة تجريبية واحدة، كل وحدة معها إعلان منشور، عشان الموقع
يبان فيه محتوى حقيقي قبل أول بائع فعلي. **مو حسابات حقيقية** — لازم تُحذف لما يسجل أول
بائع فعلي عشان ما تختلط بالمحتوى الحقيقي على الموقع الحي. طبّق
`supabase/migrations/00000000000017_demo_seed_data.sql` بـSQL Editor (زي كل الـmigrations).

## 🟡 المرحلة 2 — دفعة "الأفكار السهلة" (2026-08-16)

5 أفكار سهلة التنفيذ (بدون حاجة لتسجيل دخول مشترين، بدون شراكات، بدون فريق ميداني) نُفّذت
دفعة وحدة بطلب صريح منك:

1. **صفحات سياسات ثابتة**: `/privacy`، `/terms`، `/refund` — محتوى حقيقي (مو حشو) يعكس
   نموذج "الوسيط فقط" الفعلي. مطلوبة أصلًا لمتطلبات Tap onboarding، ومربوطة بفوتر الرئيسية.
2. **`/whats-new`** ("وش الجديد بالزلفي؟") — Feed لآخر 30 إعلان منشور بكل الفئات، مرتبة
   بتاريخ النشر. مربوطة بهيدر الرئيسية.
3. **"آخر تحديث"** على صفحة الإعلان — `lib/relative-time.ts` جديد (منسّق نسبي عربي بسيط،
   بدون مكتبة خارجية) يعرض `updated_at` الموجود أصلًا بالجدول.
4. **إحصائيات تفاعل علنية** على صفحة الإعلان — عدد المشاهدات وعدد نقرات التواصل
   (`view_count`/`contact_click_count` كانا موجودين أصلًا بالجدول من المرحلة 1، بس ما كانا
   يُعرضان بالواجهة).
5. **"رشّح مشروعًا"** — `/refer-a-business` نموذج عام (بدون تسجيل دخول) + جدول `referrals`
   جديد (migration 18، ⚠️ **غير مطبّقة بعد**) + صفحة أدمن `/admin/referrals` للمراجعة
   ("تواصلت معه" / "تجاهل"). أهم توصية فردية من ChatGPT بعصف PLAN.md §20 — أغلب الأسر
   المنتجة لن تبحث بنفسها عن التسجيل، فتحتاج قناة يجيبها المجتمع.

**اختُبر فعليًا محليًا:** `tsc`/`build` نظيفين (25 صفحة)، `npm run dev` وفحص HTTP مباشر لكل
صفحة جديدة (200 لكل الصفحات العامة، 307 لـ`/admin/referrals` بدون تسجيل دخول)، والـsitemap
يشمل الروابط الجديدة كلها. صفر أخطاء بسجل التشغيل.

⚠️ **`referrals` (migration 18) غير مطبّقة بعد** — لازم تُلصق بـSQL Editor قبل ما نموذج
"رشّح مشروعًا" يشتغل فعليًا (حاليًا يرجع خطأ "table not found" لو حد جرّب يرسل).

**قيد معروف ما اتحل (نطاق "سهل" لا يشمله):** ما فيه حد يومي على إرسال `/refer-a-business` —
شخص يقدر يرسل ترشيحات بلا حدود. أثر محدود (أسوأ حالة: ازدحام بطابور مراجعة الأدمن، مو تلاعب
بإحصائية عامة زي مشكلة عدّادات المشاهدة السابقة)، فأجّلناه عمدًا خارج نطاق الدفعة السهلة.

## 🟠 الأفكار المعقّدة الخمس (2026-08-16)

نُفّذت كلها دفعة وحدة بترتيب تصاعدي بالتعقيد، بطلب صريح منك. **الكود كامل ومُختبر
(`tsc`/`build` نظيفين، 32 صفحة، صفر أخطاء تشغيل)، لكن الـmigrations 19-23 تحتاج تطبيق يدوي
بـSQL Editor قبل ما أي وحدة منها تشتغل فعليًا.**

### 1. رحلات الاحتياج — `/journey/[slug]`
تجميع كل ما تحتاجه مناسبة واحدة بصفحة وحدة (PLAN.md §20.38-41). 4 رحلات مزروعة: أجهز
زواجي، بيتي الجديد، مولود جديد، العودة للمدارس. **قرار تصميم:** كل خطوة تحمل `search_query`
مو `category_id` فقط — عندنا 5 فئات عريضة بس، و"مصور" و"قاعة أفراح" كلاهما تحت "خدمات"، فما
يفرقهم إلا البحث العربي المطبَّع الموجود أصلًا (migration 6). الخطوة الفاضية تعرض رابط
"رشّح مشروعًا" — تحوّل الفجوة لفرصة اكتشاف بدل رسالة خطأ.

### 2. رعاية موسمية — `/admin/sponsorships`
أكثر فكرة تسييل تكررت عبر كل المصادر (§16.2، §17.4-5، §18.4، §19.2، §20.6). راعٍ لقسم أو
رحلة أو الرئيسية لفترة محددة. **تُدار يدويًا بدون دفع ذاتي عمدًا** — Tap مؤجّل، والبيع
وجهًا لوجه بهالمرحلة أصلًا. البانر يظهر بوضوح كـ"برعاية" ومعه `rel="sponsored"` — ما يتنكر
كمحتوى عادي (PLAN.md §18.4 قدّمها كدعم مجتمعي مو إعلان مزعج).

### 3. زر "أحتاج" — `/needs`، `/needs/new`، `/dashboard/needs`
**قلب النموذج**: بدل ما البائع ينشر وينتظر، الساكن ينشر احتياجه والبائعون المعتمدون يردّون.
تكررت بـ5 مصادر وأقوى توصية فردية من ChatGPT (§20.28). المشتري يبقى **بدون حساب** (يطابق
قرار TECH.md §4) — يترك رقم واتساب. حد يومي 3 طلبات لكل زائر عبر `can_post_need_request`
(نفس أسلوب HMAC للـIP من migration 16). الرد محصور بالبائع المعتمد وبطلب مفتوح غير منتهي —
مفروض بـRLS مو بالواجهة، وقيد `unique(request_id, seller_id)` يمنع تكرار الرد.

### 4. نبض الزلفي — `/admin/pulse`
سجل بحث + 3 تقارير (§19.5، §20.7-8، §20.43). **أهمها "ماذا ينقص الزلفي؟"**: كلمات بحث
رجعت صفر نتائج — أوضح مؤشر على فرصة تجارية ناقصة بالبلد. **الخصوصية:** `search_log` يخزن نص
البحث وعدد النتائج فقط — لا IP ولا visitor hash ولا user id، ما يربط بحث بشخص إطلاقًا.
دوال التجميع `SECURITY DEFINER` لكن **كل وحدة تعيد فحص `is_admin()` بنفسها** — بدونها أي
مستخدم مسجّل يقدر يستدعي الـRPC مباشرة ويقرأ التقرير كامل (الدالة تتجاوز RLS بطبيعتها).

### 5. حسابات مشترين + تقييمات موثقة — `/my/transactions`، `/dashboard/transactions`
أكبر تغيير معماري: أول ميزة تتطلب حساب للمشتري. **المشكلة الحقيقية بمجتمع صغير مو نقص
النجوم — هي إن النجوم المجهولة ما تعني شي والكل يعرف الكل.** فالتقييم هنا يتطلب تعامل
**أكّده البائع نفسه**: المشتري يضغط "تعاملت مع هذا البائع" → البائع يأكد أو ينفي → التقييم
يفتح فقط بعد التأكيد. مفروض بـRLS: سياسة إدراج التقييم تتحقق إن التعامل مؤكد ويخص هذا
المشتري تحديدًا، فما ينلف بنداء API مباشر. `revoke update (buyer_id, seller_id, listing_id)`
يمنع البائع من إعادة كتابة ملكية التعامل (نفس درس migration 15: `WITH CHECK` وحده ما يقدر
يثبّت عمود على قيمته القديمة). المتوسط والعدد يظهران بصفحة البائع وصفحة الإعلان.

### ملاحظات تقنية
- `lib/visitor-hash.ts` جديد: وحّد تهشير الـIP بين Route Handlers (تستقبل `Request`) وServer
  Actions (تقرأ `headers()`) — كان مكرر داخل `record-interaction.ts` فقط.
- `/my/*` أُضيفت لـ`robots.ts` و`middleware.ts` (منطقة خاصة).
- هيدر الأدمن أُعيد كتابته ليُبنى من قائمة بدل شرط لكل تبويب — صار 5 تبويبات.
- **أُصلح قيد حرج بـmigration 18 قبل تطبيقها:** `anon` عنده SELECT فقط على الجداول الجديدة
  (بعد تضييق migration 15)، فسياسة INSERT وحدها ما تكفي — الإدراج يفشل بـ"permission denied"
  قبل ما تُفحص RLS أصلًا. أُضيف `grant insert` صريح لـ`referrals` و`need_requests` و`search_log`.

## 🟣 الدفعة المعقّدة الثانية (2026-08-16)

خمس أفكار إضافية بنفس النظام (تصاعدي بالتعقيد، دفعة وحدة). **الكود كامل ومُختبر
(`tsc`/`build` نظيفين، 38 صفحة، صفر أخطاء تشغيل)، والهجرات 24-28 تحتاج تطبيق يدوي.**

### 1. عروض اليوم — `/offers`، `/dashboard/offers`، `/admin/offers`
عروض محدودة بوقت (PLAN.md §2.8، §1.2) — سبب زيارة يومي. **قرار تصميم:** الانتهاء مفروض
بسياسة RLS نفسها (`now() < ends_at`) مو بوظيفة تنظيف مجدولة — العرض يختفي بلحظة انتهائه بدون
أي job يشتغل، وهذا يطابق مبدأ "لا خدمات مدفوعة" (cron خارجي كان بيكون اعتماد إضافي). العرض
يمر بنفس المراجعة اليدوية كالإعلانات — ادعاء خصم غير مراجع على الواجهة يضر الثقة بمدينة صغيرة.

### 2. توصية الجار — زر بصفحة البائع
ساكن مسجّل يزكّي بائع يعرفه شخصيًا (§17.7، §18.10، §19.9، §20.20). **منفصلة عن التقييمات
عمدًا:** التقييم يتطلب تعامل مؤكد، التوصية تتطلب معرفة شخصية فقط — يجاوبون سؤالين مختلفين
("شغله زين؟" مقابل "هو شخص حقيقي معروف؟"). منع التزكية الذاتية مفروض بـ`CHECK` constraint مو
بالواجهة فقط، عشان يصمد حتى أمام إدراج من أدمن.

### 3. اسأل أهل الزلفي — `/ask`
أسئلة وأجوبة (§11.2، §20.24). **الفكرة الجوهرية:** كل رد يقدر يربط ببائع حقيقي على المنصة
(`recommended_seller_id`) — يحوّل التوصية من كلام يضيع بقروب واتساب إلى رابط دائم لصفحة
البائع. السؤال يتطلب تسجيل دخول (حسابات المشترين صارت موجودة من migration 23) لأن لوحة
أسئلة مجهولة بمدينة صغيرة مشكلة إشراف. عدّاد الردود محدّث بـtrigger (نفس نمط
`active_listings_count`).

### 4. مستويات الثقة — شارة بصفحة البائع والإعلان
بدل شارة "موثّق" غامضة، أربع مستويات متدرجة (§20.16) **كل وحدة مكتسبة من إشارة موجودة
بقاعدة البيانات فعلًا** مو من ادعاء ذاتي:
`1 مسجّل` (اعتماد أدمن) → `2 نشِط` (عنده إعلانات منشورة) → `3 موصى به` (3+ توصيات جيران) →
`4 موثّق بتعاملات` (3+ تعاملات مؤكدة من الطرفين وتقييم 4.0+). `identity_verified` عمود
منفصل يضبطه الأدمن فقط (revoke على العمود) ويُعرض جنبها لأنه يجاوب سؤال مختلف. **الشارة
بتنسيق محايد عمدًا** — شارة ملونة تُوحي بضمان المنصة لا تقدمه (وسيط فقط، حسب صفحة الشروط).

### 5. إشعارات داخلية — `/notifications` + 7 triggers
كل ميزة بنيناها تنتج أحداث كان البائع يكتشفها بإعادة فحص الصفحات يدويًا. **داخل التطبيق مو
بريد/SMS عمدًا:** مزود بريد اعتماد مدفوع، والمشروع ما يدفع قبل الدخل (TECH.md مبدأ التكلفة) —
جدول إشعارات مجاني وهو الأساس الصحيح لإضافة البريد فوقه لاحقًا. 7 triggers: اعتماد/رفض إعلان،
اعتماد/رفض حساب، مطالبة تعامل، تقييم جديد، توصية جار، رد على سؤالك، اعتماد/رفض عرض. **كلها
`SECURITY DEFINER` بـ`search_path` مثبّت** لأنها تُدرج صفًا لمستخدم *غير* اللي سوّى الإجراء —
وRLS كان بيرفض ذلك. المستخدم يقدر يعدّل `is_read` فقط (revoke على باقي الأعمدة).

### ملاحظات
- `admin_actions` توسّعت لتشمل `offer` (migration 24 يوسّع الـcheck constraint اللي وسّعه 18).
- هيدر لوحة البائع صار يقرأ عدّاد الإشعارات غير المقروءة بنفسه بدل تمريره prop لكل صفحة.
- `/notifications` و`/ask/new` أُضيفتا لـ`robots.ts`، و`/ask` و`/seller` لـ`middleware` (صارت
  تحتاج جلسة للتوصية والرد).

## خطوات أول جلسة على أي جهاز جديد

1. `git pull origin main`
2. `npm install`
3. أنشئ `.env.local` من `.env.local.example` (القيم الحقيقية محفوظة بمحادثة سابقة معك — لا ترفعها لـGit أبدًا)
4. تأكد كل الـmigrations مطبّقة على مشروع Supabase (كلها انطبقت فعليًا — راجع الجدول تحت)
5. `npm run dev`

## ✅ خلّصناها

### الصفحات العامة
- [x] `/` — الرئيسية: تصفح الفئات الخمس من `categories`
- [x] `/category/[slug]` — إعلانات الفئة المنشورة (المميزة أولًا ثم الأحدث)
- [x] `/listing/[slug]` — تفاصيل الإعلان + زر واتساب + تتبع النقرات والمشاهدات
- [x] `/seller/[slug]` — صفحة البائع المعتمد وإعلاناته
- [x] `/search?q=` — بحث عربي مطبَّع (يطابق كيكه/كيكة) عبر `pg_trgm`

### تحسين محركات البحث (SEO)
- [x] `sitemap.xml` ديناميكي (الفئات + البائعون المعتمدون + الإعلانات المنشورة)
- [x] `robots.txt` يستبعد `/dashboard /admin /api /auth /login /search`
- [x] `generateMetadata` لكل صفحة عامة — عنوان ووصف خاص بكل إعلان/بائع/فئة

### المصادقة والبائع
- [x] `/login` + `/auth/callback` — دخول برابط بريد (بدون كلمة مرور) عبر Supabase Auth
- [x] `/dashboard/setup` — إنشاء بيانات النشاط (يروح `pending`)
- [x] `/dashboard` — إعلانات البائع وحالتها وعدّاد الحد المجاني
- [x] `/dashboard/listings/new` — إضافة إعلان (يروح `pending_review`)
- [x] `/dashboard/listings/[id]/edit` — تعديل الإعلان (يرجّعه للمراجعة)
- [x] أرشفة إعلان (soft delete، بدل حذف فعلي)
- [x] رفع الصور: ضغط WebP بالعميل → Supabase Storage → حذف/ترتيب (حد 8 صور/إعلان)
- [x] حد يومي 3 إعلانات/بائع (PLAN §4.5) — مطبّق بـRLS، ما ينلف بالـAPI مباشرة

### لوحة الإدارة
- [x] `/admin/sellers` — اعتماد/رفض البائعين الجدد
- [x] `/admin/listings` — اعتماد/رفض الإعلانات
- [x] سجل `admin_actions` (audit log) لكل قرار إداري

### قاعدة البيانات (`supabase/migrations/`) — كلها مطبّقة فعليًا على المشروع
| # | الملف | المحتوى |
|---|---|---|
| 1 | `initial_schema` | كل الجداول + RLS + triggers + `can_create_listing` |
| 2 | `contact_click_rpc` | `record_contact_click` — تسجيل نقرة واتساب |
| 3 | `admin_actions` | سجل إجراءات الإدارة (TECH.md §12.3) |
| 4 | `profile_on_signup` | إنشاء `profiles` تلقائيًا عند التسجيل |
| 5 | `fix_admin_policy_recursion` | إصلاح تكرار لانهائي بسياسات الأدمن |
| 6 | `search_listings` | دالة البحث العربي المطبَّع |
| 7 | `view_count` | `record_listing_view` — تسجيل مشاهدة إعلان |
| 9 | `listing_images_storage` | bucket صور الإعلانات + RLS على `storage.objects` |
| 10 | `daily_listing_limit` | `can_create_listing_today` — حد 3 إعلانات جديدة باليوم |
| 11 | `grant_base_privileges` | GRANT أساسي لـanon/authenticated (كان ناقص افتراضيًا بالمشروع) |
| 12 | `public_read_reference_tables` | RLS + سياسة قراءة عامة لـcategories/regions/neighborhoods/plans |
| 13 | `seed_plan` | زرع خطة الاشتراك (99 ر.س/شهر، مؤقت) |
| 14 | `zulfi_neighborhoods` | 26 حي حقيقي بالزلفي (بيانات من صاحب المشروع) |
| 15 | `security_hardening` | إصلاح مراجعة الكود الشاملة — تفاصيل تحت |
| 16 | `interaction_rate_limit` | ✅ حد يومي على عدّادات المشاهدة/التواصل — مطبّقة (2026-08-15) |
| 17 | `demo_seed_data` | ⚠️ **غير مطبّقة** — بائع وأسرة منتجة تجريبيين للعرض |
| 18 | `referrals` | ⚠️ **غير مطبّقة** — جدول "رشّح مشروعًا" + توسيع `admin_actions` |
| 19 | `need_journeys` | ⚠️ **غير مطبّقة** — رحلات الاحتياج + 4 رحلات مزروعة |
| 20 | `sponsorships` | ⚠️ **غير مطبّقة** — رعاية الأقسام والرحلات |
| 21 | `need_requests` | ⚠️ **غير مطبّقة** — زر "أحتاج" (طلبات + ردود البائعين) |
| 22 | `market_pulse` | ⚠️ **غير مطبّقة** — سجل البحث + تقارير نبض الزلفي |
| 23 | `verified_reviews` | ⚠️ **غير مطبّقة** — تعاملات موثقة + تقييمات |
| 24 | `daily_offers` | ⚠️ **غير مطبّقة** — عروض محدودة بوقت |
| 25 | `vouches` | ⚠️ **غير مطبّقة** — توصية الجار |
| 26 | `community_qa` | ⚠️ **غير مطبّقة** — اسأل أهل الزلفي |
| 27 | `trust_levels` | ⚠️ **غير مطبّقة** — مستويات الثقة المحسوبة |
| 28 | `notifications` | ⚠️ **غير مطبّقة** — إشعارات داخلية + 7 triggers |

*(رقم 8 محذوف عمدًا — كان بيانات أحياء مخترعة، اتشال قبل التطبيق)*

### ملاحظة تشغيلية: `INTERACTION_HASH_SECRET`
موجود محليًا بـ`.env.local`. **لما ينشر الموقع فعليًا (Vercel أو غيره)، لازم يُعبّى بنفس المتغير
هناك أيضًا** — بدونه الحد اليومي يشتغل لكن كل الزوار يُعاملون كزائر واحد لأن السري يسقط لقيمة
افتراضية ثابتة محليًا فقط.

## 🔍 مراجعة كود شاملة (2026-08-15)

راجعت 8 وكلاء متخصصين كل الكود من بداية المشروع (`git diff` من أول commit). لقوا مشاكل حقيقية،
كلها انصلحت بـmigration 15 + تعديلات كود مرافقة:

**أمنية حرجة (كانت مفتوحة فعليًا بالإنتاج):**
- دوال `SECURITY DEFINER` (`is_admin`, `can_create_listing`, وغيرها) بدون `search_path` ثابت —
  ثغرة انتحال جدول مؤقت (temp table shadowing) تتجاوز فحص الأدمن والحد اليومي بالكامل
- `listings_update_own` و`sellers_update_own` بدون `WITH CHECK` — أي بائع يقدر يعتمد حسابه
  بنفسه أو ينشر إعلانه مباشرة عبر Supabase client بدون مراجعة أدمن
- `GRANT ALL` (migration 11) كان يشمل `TRUNCATE` غير الخاضع لـRLS إطلاقًا — ضُيّق لصلاحيات محددة

**تصحيحية عالية:**
- نظام الدفع (Tap) كامل كان **ما يقدر يشتغل أبدًا** — ما فيه سياسة INSERT على `subscriptions`/`payments`
- مفتاح idempotency بـwebhook Tap كان يبلع أول webhook حقيقي (`CAPTURED`) كـ"مكرر" ولا يفعّل الاشتراك
- `listings.slug` بدون unique constraint، وحد الصور الثماني بدون قفل ذري (سباق حالة ممكن)

**تبسيط وكفاءة:** توحيد استعلامات `generateMetadata` المكررة، دمج مسارَي التفاعل المتطابقين،
هيدر مشترك للوحتين (كان مكرر بـ6 صفحات)، تضييق `middleware` عشان ما يستدعي Auth بالصفحات العامة.

راجع commit `a6e7b9d` للتفاصيل الكاملة.

## 🔍 مراجعة واختبار كامل (2026-08-15، الجلسة الثانية — الجهاز الشخصي)

تشغيل واختبار فعلي كامل بعد سحب شغل جهاز العمل: `tsc --noEmit` (صفر أخطاء)، `npm run build`
(19 صفحة، صفر أخطاء)، وتشغيل `npm run dev` مع فحص كل المسارات بـHTTP فعليًا:

| المسار | متوقع | فعلي |
|---|---|---|
| `/` | 200 + الفئات من Supabase | ✅ الفئات الخمس ترجع فعليًا |
| `/category/[slug]`، `/search`، `/sitemap.xml`، `/robots.txt` | 200 | ✅ |
| `/dashboard`، `/admin/*` | 307 → `/login` | ✅ |
| `/listing/nope`، `/seller/nope` | 404 | ✅ |
| `POST /api/webhooks/tap` بدون توقيع | 401 | ✅ |

تأكدنا كمان إن `.env.local` محمي بـ`.gitignore` وما فيه أي سر مرفوع للريبو.

### أُصلحت بهذه الجلسة
- **حرج (الدفع):** الـwebhook كان يحجز مفتاح idempotency **قبل** ما يتأكد إن صف الدفعة موجود.
  لو وصل webhook قبل ما يلحق `startCheckout` يحفظ صف `payments` (الـcharge يُنشأ قبلها بأجزاء
  من الثانية)، يرجع 404 ويكون حرق المفتاح — وإعادة المحاولة من Tap تتعامل كـ"مكرر" فيبقى
  الاشتراك `pending` للأبد رغم دفع حقيقي. الحين البحث عن الدفعة صار أولًا، وأي فشل بعد حجز
  المفتاح يحرّره عشان إعادة المحاولة تشتغل.
- **تناسق:** migration 15 كان فيه `alter table ... add constraint` غير idempotent — يخالف
  قاعدتنا بالدرس رقم 5 تحت. صار داخل `do $$ ... if not exists`.
- **فشل صامت:** `archiveListing` كان يتجاهل خطأ التحديث — الأرشفة الفاشلة تبدو ناجحة تمامًا.

### ⚠️ اكتُشفت وما انصلحت بعد (قرار لك)
1. **`startCheckout` ينشئ charge بـTap قبل ما يحجز صفوف قاعدة البيانات** — لو فشل حفظ
   `subscriptions`/`payments`، يبقى charge يتيم عند Tap والمستخدم يشوف خطأ بدون طريقة استرجاع.
2. **رسالة خطأ مضللة بإضافة إعلان** — أي فشل insert (تصادم slug، حد يومي، مشكلة اتصال) يطلع
   نفس الرسالة "تأكد إنك ما تجاوزت الحد المجاني".

## 🔧 إصلاح: حد يومي على عدّادات المشاهدة/التواصل (2026-08-15، الجلسة الثالثة)

المشكلة المكتشفة بالجلسة السابقة (**ما فيه rate limit على `/api/listing-view` و
`/api/contact-click`** — مُختبرة فعليًا: 3 طلبات POST متتالية بلا تسجيل دخول كلها احتُسبت)
انصلحت بـ**migration 16** (`interaction_rate_limit`، ⚠️ غير مطبّقة بعد — راجع أعلاه):

- جدول `interaction_log` جديد: صف واحد لكل (إعلان، نوع تفاعل، زائر، يوم) بقيد `unique` —
  التكرار بنفس اليوم ينرفض بصمت بدل ما يزيد العدّاد.
- الزائر يُعرَّف بـHMAC-SHA256 لعنوان الـIP (سري جديد `INTERACTION_HASH_SECRET`) — **مو IP خام
  مخزّن**، عشان الخصوصية وعشان يصعب أي حد يعكسه لعنوان حقيقي.
- **قرار تصميم متعمد:** حل بـPostgres العادي (مجاني ضمن خطة Supabase الحالية)، مو خدمة
  Rate-Limiting/Redis مدفوعة — يطبّق مبدأ "لا ندفع فلوس على أي شي قبل ما يثبت المشروع ويصير
  عنده دخل فعلي" (طلب صريح منك بهذه الجلسة، راجع أيضًا مبدأ التكلفة بـ[TECH.md](TECH.md)).
- اختُبر محليًا: الـbuild وTypeScript نظيفين، الصفحات الثانية كلها سليمة، واستدعاء الدالة
  الجديدة يرجّع خطأ واضح (500، "function not found") بدل انهيار — متوقع لين تُطبَّق
  migration 16 يدويًا بـSQL Editor.

### تنظيف
- [x] توحيد اسم الموقع بمصدر واحد (`lib/seo.ts`) بدل تكراره بـ13 مكان
- [x] حذف بيانات أحياء مخترعة (كانت اجتهاد شخصي غير موثوق)
- [x] إصلاح أنواع TypeScript للعلاقات المتداخلة بـSupabase (اكتُشفت بأول `tsc`/`build` فعلي)

## ⏳ باقي من المرحلة 1
- [ ] **بيانات أحياء الزلفي الحقيقية** — الجدول فاضٍ، يحتاج قائمة موثوقة منك

### الدفع (Tap) — الكود جاهز، التفعيل الحقيقي معلّق عليك
- [x] `/dashboard/subscription` — عرض الخطة (99 ر.س/شهر، مؤقت) وبدء الاشتراك
- [x] `lib/payments/tap.ts` — إنشاء charge + تحقق توقيع webhook (HMAC)
- [x] `app/api/webhooks/tap` — مصدر الحقيقة الوحيد لتفعيل الاشتراك، idempotent عبر `payment_events`، مُختبر يرفض webhook بتوقيع خاطئ (401) فعليًا
- [x] الخطة مزروعة بقاعدة البيانات (id=1, 99 ر.س)
- [ ] **غير مختبر مع Tap حقيقي** — لا يوجد حساب Tap بعد (معلّق على رخصة العمل الحر). لما يتوفر الحساب: عبّي `TAP_SECRET_KEY`/`TAP_WEBHOOK_SECRET`، وتأكد كتابيًا من Tap إن recurring billing مدعوم (TECH.md §7) قبل أي اعتماد فعلي
- [ ] **التجديد التلقائي غير مبني** — الكود الحالي يفعّل اشتراك شهر واحد بعد الدفع الأول بس؛ التجديد يحتاج job مجدول يعيد تحصيل البطاقة المحفوظة، لسا ما بنيناه

## دروس مهمة من أول تشغيل فعلي (تفيد أي مشروع Supabase جديد)
1. **مشاريع Supabase الجديدة ما تمنح GRANT أساسي تلقائيًا** لـ`anon`/`authenticated` — لازم `grant all on all tables in schema public to anon, authenticated, service_role;` صراحة (migration 11).
2. **RLS يتفعّل تلقائيًا على كل جدول جديد** حتى لو ما استدعيت `enable row level security` بنفسك — أي جدول بدون policy صريحة يرجع نتائج فاضية بصمت (200 OK, بدون خطأ) لكل الأدوار ما عدا `service_role`. الجداول المرجعية العامة (categories, regions, neighborhoods, plans) احتاجت `for select using (true)` صريحة (migration 12).
3. **`npm run build` و`npm run dev` ما يشتغلون بنفس الوقت** — كلاهما يكتب على `.next` بالتزامن ويسبب تلف (`ENOENT` بملفات مؤقتة). أوقف `dev` قبل أي `build` تجريبي، أو احذف `.next` وأعد التشغيل لو صار تعارض.
4. **مفتاح `service_role` يتجاوز RLS دايمًا** — مفيد جدًا للتشخيص (لو شغّال بس anon فاضي، المشكلة صلاحيات/RLS مو بيانات).
5. **SQL Editor بـSupabase يشتغل autocommit لكل statement** (مو transaction واحدة) — لو سكربت طويل يفشل بمنتصفه، أول نص يتنفذ يبقى محفوظ. خلّينا كل migration لاحقة idempotent (`if not exists`, `on conflict do nothing`, `drop policy if exists`) عشان تتحمّل إعادة التشغيل بأمان.

## ملاحظات تقنية للجلسة الجاية
- ما فيه أنواع TypeScript مولّدة من Supabase (`database.types.ts`) — الأنواع اليدوية عبر `.returns<T>()`/`.single<T>()` شغّالة حاليًا، بس `npx supabase gen types typescript` أنضف حل طويل المدى
- `middleware.ts` طبقة راحة فقط مو حدود أمان — كل صفحة محمية تتحقق بنفسها عبر `lib/auth/permissions.ts`
- أول أدمن لازم يُرقّى يدويًا: `update profiles set role = 'admin' where id = '<user-id>';`
