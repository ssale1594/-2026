# تعليمات دائمة لهذا المشروع

## الرفع التلقائي لـ GitHub (مهم جدًا)
صاحب المشروع يشتغل وحده وبوقت متقطع (فراغ متقطع من دوامه)، وعنده جهازين (شخصي وعمل) يشتغل عليهم. **بدون ما يطلب صراحة**، بعد أي تعديل ذو معنى على الملفات (توثيق قرارات بـPLAN.md/TECH.md/ROADMAP.md، أو كود فعلي بالمشروع)، سوّي:

```
git add <الملفات المعدّلة>
git commit -m "<رسالة قصيرة تصف التغيير>"
git push origin main
```

- لا تنتظر تأكيد المستخدم لكل عملية رفع روتينية — هذا إذن دائم بالرفع التلقائي بعد أي تغيير مهم.
- استثناء: لا ترفع تلقائيًا عمليات محفوفة بالمخاطر (force push، حذف فروع، إعادة كتابة تاريخ) — تلك تحتاج تأكيد صريح دايمًا.
- تجاهل التعديلات التافهة (تعديل حرف واحد) — اجمعها مع أقرب تعديل ذو معنى بدل commit منفصل لكل شي.
- لو فيه تعارض دفع (reject) بسبب تعديلات من الجهاز الثاني، اعمل `git pull` أول (merge عادي) قبل أي محاولة push ثانية.

## القرص C ممتلئ
لا تثبّت أو تخزّن أي شي بهذا المشروع على قرص C (يبقى أقل من 3GB فاضي). استخدم D فقط. راجع تفاصيل أكثر بالذاكرة الدائمة لو احتجت.

## ملفات مرجعية بالمشروع
- [STATUS.md](STATUS.md) — **ابدأ من هنا**: وش خلص فعليًا، وش ناقص، وخطوات أول جلسة على الجهاز الشخصي
- [PLAN.md](PLAN.md) — بنك أفكار العمل (نموذج ربح، فئات، نمو...)
- [TECH.md](TECH.md) — القرارات التقنية النهائية (Supabase, Next.js, Tap...)
- [ROADMAP.md](ROADMAP.md) — ترتيب الأفكار بمراحل تنفيذ زمنية
