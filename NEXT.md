# نقطة الاستئناف — آخر تحديث 2026-08-22

> **لجلسة جديدة:** اقرأ هذا الملف أولًا (يغني عن AUDIT.md — صار لقطة تاريخية)، ثم كمّل من "المتبقي" أدناه.
> السطر الواحد اللي تبدأ فيه: *"اقرأ NEXT.md وكمّل من حيث توقفت"*.

---

## ✅ كل الهجرات مطبّقة على الإنتاج فعليًا (لا حاجة تلصق أي شي)

بعد اتصال Supabase MCP بهذي الجلسة، طُبّقت 8 هجرات مباشرة على قاعدة الإنتاج
(`ownxrmyxbryizpynrzrb`) — لا نسخ ولا لصق، وكل واحدة اتحقق نجاحها فعليًا:

| # | الهجرة | ملف محلي |
|---|---|---|
| 1 | إصلاح مسارات الكتابة الإدارية (اعتماد بائع، تدقيق، استفتاءات، انتهاء اشتراك) | `migrations/…54` |
| 2 | الموقع الجغرافي + دليل الخريطة | `migrations/…55` |
| 3 | سفراء الأحياء | `migrations/…57` |
| 4 | النشرة الأسبوعية | `migrations/…58` |
| 5 | إغلاق حلقة رشّح مشروعًا | `migrations/…59` |
| 6 | الدليل العام للزلفي | `migrations/…60` |
| 7 | تصليب صلاحيات الدوال (123 دالة) + search_path لـ10 دوال trigger | `migrations/…61` |
| 8 | 3 عطول حرجة انكشفت بمراجعة شاملة لكل القاعدة (تفصيل بالأسفل) | `migrations/…62` |

**اختبرت `admin_set_seller_verification` حيًا بمحاكاة جلسة حسابك الأدمن الفعلي — نجح، والحالة تحدّثت، وسجل التدقيق كتب صح.**

---

## 🔍 مراجعة شاملة لكل قاعدة Supabase (2026-08-22) — 3 أعطال حقيقية جديدة

طلب صاحب المشروع "راجع القديم وصححه" — فحصت الأمان والأداء بأدوات Supabase
الرسمية (`get_advisors`) + راجعت كل سياسات RLS (170+ سياسة) يدويًا. النتيجة:

### 🔴 حرجة (أُصلحت فورًا)
1. **`sellers.active_listings_count` قابل للتعديل المباشر من أي بائع** — سابق
   لليلة، فات إدراجه بقائمة الأعمدة المحجوبة بالهجرة 53. أي بائع يقدر
   `UPDATE sellers SET active_listings_count = 0` ويتجاوز حد الفريميوم
   بالكامل. **الأخطر بين كل ما وُجد الليلة** — يبطل نموذج الربح الأساسي.
2. **سياسة `vouches` تحوي تناقضًا منطقيًا (`seller_id = seller_id`)** — يسمح
   لأي مستخدم زكّى بائعًا يعيد توجيه تزكيته لبائع مختلف تمامًا خلال 30 يوم
   (تزكية منتحلة). موجود منذ إنشاء الجدول، لا علاقة له بالليلة.
3. **أعمدة الموقع الجديدة (`latitude`, `longitude`, `phone`,...) بلا صلاحية
   UPDATE إطلاقًا** — من عندي الليلة: عمود جديد على جدول عليه أصلاً صلاحيات
   أعمدة مقيّدة (`sellers`) لا يرث صلاحية الكتابة تلقائيًا. **كانت
   `/dashboard/location` معطّلة بالكامل بالإنتاج (42501) من ثواني الإطلاق**
   — ما انكشفت لأن اختباري السابق كان بصلاحيات `service_role` (تتجاوز
   القيود) لا بجلسة بائع حقيقية. **درس مهم لأي هجرة جديدة تضيف عمودًا على
   `sellers`/`listings`/أي جدول من قائمة الهجرة 53: لازم `grant update
   (العمود) ... to authenticated` صريح، منفصل عن `alter table add column`.**

### ✅ فُحص وتأكد أنه سليم (لا حاجة تعديل)
- `deals.status` غير محجوب على مستوى العمود، **لكن** `deals_status_guard`
  (trigger) يفرض آلة حالة صارمة لكل انتقال — الحماية موجودة بطبقة أخرى، صحّ.
- 123 دالة `SECURITY DEFINER` قابلة نظريًا للاستدعاء من `anon` — لكن Supabase
  نفسها تمنح هذا تلقائيًا كجزء من تعريض RPC (سلوك منصّة، مو ثغرة)، والحماية
  الفعلية (`is_admin()`/`auth.uid()` بداخل كل دالة) مُتحقَّقة وتعمل.
- كل جدول عليه RLS مفعّل (54 جدول)، ولا سياسة SELECT/UPDATE عامة أوسع من
  المفروض بأي جدول حساس.
- `rls_enabled_no_policy` على 3 جداول (`interaction_log`,
  `newsletter_subscribers`, `payment_events`) — **مقصود**: صفر سياسات يعني
  حظر كامل لغير `SECURITY DEFINER`/`service_role`، موثّق بتعليقات كل هجرة.

### 🟡 منخفضة الأولوية — لم تُلمس عمدًا
- `extension_in_public` (`pg_trgm`, `btree_gist`) — نقلها لسكيمة منفصلة
  عملية حساسة قد تكسر فهارس `gin_trgm_ops` الحالية (بحث الإعلانات). خطورة
  فعلية منخفضة جدًا، لا تستحق المخاطرة على قاعدة حية بهذا الحجم الصغير.
- `auth_leaked_password_protection` — إعداد بلوحة Supabase (Auth settings)،
  غير ذي صلة عمليًا لأن الدخول بالمشروع كله رابط سحري بدون كلمة مرور.
- أداء (`auth_rls_initplan` ×82، `multiple_permissive_policies` ×348،
  `unindexed_foreign_keys` ×50) — كلها WARN/INFO تتعلق بالأداء عند حجم بيانات
  كبير، لا خطورة أمنية. القاعدة الآن بضع عشرات صفوف فقط — تحسينها الآن
  تحسين سابق لأوانه. أعد تقييمها بعد أول 1000+ إعلان حقيقي.

---

## 🔴 مطلوب من صاحب المشروع (الوحيد المتبقي)

### 1. أضف `CRON_SECRET` على Vercel
```
npx vercel env add CRON_SECRET production
```
القيمة: أي نص عشوائي 32 حرف فأكثر. بدونه المهام المجدولة (انتهاء الاشتراك،
البريد، النشرة) ترفض التشغيل عمدًا.

### 2. تحقق من صفحة Cron Jobs بلوحة Vercel
https://vercel.com/salems-projects-514652d7/2026/settings/cron-jobs — تأكد
الثلاثة مفعّلة فعليًا (باقة Hobby قد تحدّد العدد أو التكرار).

### 3. اختياري: مزوّد بريد
`npx vercel env add RESEND_API_KEY production` — بدونه الإشعارات ونشرة "وش
الجديد" تبقى بلا إرسال فعلي (الكود جاهز ومنتظر).

### 4. موقوف خارجيًا: Tap
`TAP_SECRET_KEY` و`TAP_WEBHOOK_SECRET` — معلّقة على رخصة العمل الحر.

### 5. قرار منك لاحقًا: حذف البيانات التجريبية
اتركها لين يسجّل أول بائع حقيقي بإعلان منشور، وبعدها الصق
`supabase/migrations/00000000000056_remove_demo_data.sql`.

---

## ✅ ملخص كامل — كل شي اتنفّذ منذ بداية الجلسة

**إصلاحات حرجة (كانت تمنع الإطلاق):** اعتماد البائع، فحص أخطاء الإجراءات
الإدارية، سجل التدقيق، `rejection_reason`، حظر البائع، لوحة التدقيق،
الإبلاغ عن إعلان، الاستفتاءات (مرتين — كسر أول استفتاء + منح service_role
فقط)، انتهاء الاشتراك (+ خلل ترتيب وجدته واصلحته قبل التطبيق)، عامل البريد،
حماية فجوة النشر.

**تحسينات:** همبرغر الجوال (لوحة البائع 15 رابط + الأدمن 13)، توحيد أسباب
البلاغ، نقل `app/deals/`.

**5 أفكار معقّدة جديدة من PLAN.md:** دليل الخريطة، ملصق QR، سفراء الأحياء،
النشرة الأسبوعية، إغلاق حلقة الترشيح، **+ السادسة: الدليل العام للزلفي**
(حل مشكلة البيضة والدجاجة). **26 فكرة معقّدة مبنية من 26** (عدا عزل الدِيَر
المؤجّل بقرار، وشبكة التوصيل/Escrow المؤجّلة — تحتاج ترخيص ساما).

**مراجعة شاملة لكل القاعدة:** 3 أعطال حرجة إضافية (عدّاد الإعلانات القابل
للتلاعب، تناقض منطقي بتزكيات الجيران، أعمدة الموقع المعطّلة) + تصليب صلاحيات
123 دالة + تثبيت search_path لـ10 دوال — كلها مطبّقة ومُختبرة على الإنتاج.

**الحالة:** الموقع الحي فُحص بعد كل تغيير (200/307 طبيعي بكل الصفحات)، ولا
شي متكسّر. كل الكود مرفوع على GitHub بأكثر من 30 commit الليلة.

---

## ⏭️ المتبقي — أولوية منخفضة، مو حرجة

- [ ] `NEXT_PUBLIC_SITE_URL` فاضي بـ`.env.local` محليًا (مضبوط بـVercel، محلي فقط)
- [ ] تقليل `as any` (213 موضع) — ولّد أنواع Supabase (`supabase gen types typescript`)
- [ ] تقسيم الملفات الضخمة (`moderation-client.tsx` 607 سطر، `seller/[slug]/page.tsx` 555 سطر)
- [ ] بعد أول بائعين حقيقيين: اختبار كل تدفقات الكتابة الفعلية بجلسة بائع حقيقية
      لا `service_role` فقط — درس الليلة (خطأ رقم 3 أعلاه) أن اختبار
      service_role وحده لا يكشف أعطال صلاحيات العمود
- [ ] بعد نمو البيانات: مراجعة أداء (`auth_rls_initplan`, `multiple_permissive_policies`)

---

## ملاحظات تشغيلية

- **لا تحفظ أي شي على قرص C** — استخدم D فقط (القرص C أقل من 3GB فاضي).
- الرفع التلقائي لـGitHub بعد كل تعديل ذو معنى (CLAUDE.md) — بدون انتظار تأكيد.
- **Supabase MCP متصل الآن** — استخدم `apply_migration`/`execute_sql` مباشرة
  بدل النسخ واللصق اليدوي. `project_id`: `ownxrmyxbryizpynrzrb`.
- بعد أي هجرة جديدة تضيف عمودًا على جدول من قائمة الهجرة 53 المقيّدة
  (`listings`, `sellers`, `transactions`, `offers`, `questions`, `answers`,
  `notifications`, `events`, `jobs`, `deals`, `seller_bookings`, `vouches`) —
  **لازم `grant update (العمود الجديد) ... to authenticated` صريح** إن كان
  المفروض يكون قابلاً للتعديل الذاتي، وإلا يبقى محجوبًا بصمت (درس خطأ #3).
- `npm run build` و`npm run dev` ما يشتغلون بنفس الوقت (يفسدون `.next`).
