# نقطة الاستئناف — آخر تحديث 2026-08-22

> **لجلسة جديدة:** اقرأ هذا الملف أولًا (يغني عن AUDIT.md — صار لقطة تاريخية)، ثم كمّل من "المتبقي" أدناه.
> السطر الواحد اللي تبدأ فيه: *"اقرأ NEXT.md وكمّل من حيث توقفت"*.

---

## 🔴 مطلوب من صاحب المشروع (لا أقدر أسويه بنفسي)

### 1. طبّق 6 هجرات على Supabase — **الأهم، ما فيه شي جديد يشتغل بدونها**
افتح [Supabase SQL Editor](https://supabase.com/dashboard/project/ownxrmyxbryizpynrzrb/sql) والصق
كل ملف من التالية **بلصقة منفصلة** (Run، تأكد "Success"، بعدها الملف التالي) **بهذا الترتيب بالضبط**:

1. `supabase/migrations/00000000000054_admin_write_paths.sql`
2. `supabase/migrations/00000000000055_seller_location.sql`
3. `supabase/migrations/00000000000057_neighborhood_ambassadors.sql`
4. `supabase/migrations/00000000000058_newsletter.sql`
5. `supabase/migrations/00000000000059_referral_followup.sql`
6. `supabase/migrations/00000000000060_public_directory.sql`

⚠️ كل ملف **كاملًا دفعة واحدة** — محرر Supabase ينفّذ اللصقة كمعاملة واحدة، فأي خطأ يلغي كل شي
بتلك اللصقة فقط (لا يؤثر على لصقات سابقة نجحت). لو طلع خطأ، انسخ نصه وأعطني إياه.

**قبل التطبيق:** لوحة الإدارة، الخريطة، سفراء الأحياء، والنشرة ترجع خطأ عند أي استخدام فعلي —
لكن الصفحات نفسها تفتح بدون كسار (الكود مكتوب يتحمّل غياب الهجرات). **بعد التطبيق:** كل شي
يشتغل فورًا بدون إعادة نشر.

*(الهجرة 56 استثناء — لا تُطبَّق الآن، اقرأ البند 4 بالأسفل.)*

### 2. تحقق من صفحة Cron Jobs بلوحة Vercel
صار عندنا 3 مهام مجدولة بـ`vercel.json` (انتهاء الاشتراك يوميًا، البريد يوميًا، النشرة أسبوعيًا).
النشر نجح بدون خطأ لكن تأكد بنفسك من
https://vercel.com/salems-projects-514652d7/2026/settings/cron-jobs
إن الثلاثة مفعّلة فعليًا (باقة Hobby قد تحدّد العدد أو التكرار).

### 3. أضف متغيّر `CRON_SECRET` على Vercel
```
npx vercel env add CRON_SECRET production
```
القيمة: أي نص عشوائي 32 حرف فأكثر. بدونه المهام المجدولة ترفض التشغيل عمدًا (أفضل من مسار
مفتوح يعدّل الإعلانات ويرسل بريد بدون حماية).

### 4. قرار منك: حذف البيانات التجريبية (لاحقًا، ليس الآن)
`demo-shop` و`demo-home-producer` وإعلاناتهما هم **المحتوى الوحيد** بالموقع حاليًا. حذفهم الآن
يخلّي الموقع فاضيًا تمامًا. اتركهم لين يسجّل أول بائع حقيقي بإعلان منشور، وبعدها الصق
`supabase/migrations/00000000000056_remove_demo_data.sql` (فيها حارس يرفض التنفيذ لو ما فيه
بائع حقيقي بعد).

### 5. اختياري: مزوّد بريد
`npx vercel env add RESEND_API_KEY production` — بدونه الإشعارات ونشرة "وش الجديد" الأسبوعية
تبقى بلا إرسال فعلي (الكود جاهز ومنتظر).

### 6. موقوف خارجيًا: Tap
`TAP_SECRET_KEY` و`TAP_WEBHOOK_SECRET` — معلّقة على رخصة العمل الحر. لا شي أسويه هنا.

---

## ✅ خلص الليلة (كله مرفوع على GitHub)

**إصلاحات حرجة (كانت تمنع الإطلاق):**
1. اعتماد البائع — 9 دوال `SECURITY DEFINER` بدل UPDATE مباشر كان يرجع `42501` (`migrations/…54`, `app/admin/actions.ts`)
2. فحص أخطاء بكل إجراء إداري بدل ابتلاعها بصمت مع كتابة سجل تدقيق كاذب (`app/admin/actions.ts`, `review-buttons.tsx`)
3. `admin_actions` — نوع `target_id` من `uuid` إلى `text`، `target_type` موسّع (كل سطر تدقيق كان يفشل)
4. عمود `rejection_reason` الناقص على `listings`/`sellers`
5. حظر البائع كان يحدّث أعمدة بجدول خطأ (`profiles` بدل `sellers`) — دالة `admin_ban_seller`
6. لوحة التدقيق كانت ميتة بالكامل — سياسة SELECT للأدمن + منح `moderation_stats()`
7. الإبلاغ عن إعلان يفشل دائمًا — `target_id` كان `bigint` مقابل `listings.id` وهو `uuid`
8. `/polls` تنكسر بأول استفتاء — `poll_results()` كانت تقرأ أعمدة من جدول خطأ
9. أزرار الاستفتاءات ممنوحة لـ`service_role` فقط بدل `authenticated`
10. **انتهاء الاشتراك** — `expire_due_subscriptions()` + cron يومي (لم يكن منفّذًا إطلاقًا)
11. عامل البريد يستخدم عميل الجلسة بدل service_role → صفر إيميل مرسل دائمًا
12. حماية فجوة النشر: صفحة البائع كانت ترجع 404 حتى تُطبَّق الهجرة يدويًا (`lib/data/sellers.ts`)
13. **خلل منطقي وجدته بمراجعتي الذاتية قبل التطبيق**: مهمة إيقاف الإعلانات كانت تُرتّب بعكس المقصود — توقف أقدم الإعلانات (الأكثر رسوخًا بالبحث) بدل أحدثها. أُصلح قبل أن يصل لأي بائع حقيقي.

**تحسينات وواجهة:**
14. همبرغر للجوال بلوحة البائع (13 رابط) ولوحة الأدمن (11 رابط) — `components/nav-menu.tsx`
15. توحيد قائمة أسباب البلاغ بملف واحد بدل نسختين متطابقتين تقريبًا
16. نقل `app/deals/` → `components/deals/` (كان مجلد route بلا `page.tsx`)
17. تتبع نقرة واتساب بصفحة البائع (كانت بلا تتبع إطلاقًا رغم أنه حجّة البيع للبائع)

**أفكار معقّدة جديدة (من IDEAS.md، كلها الآن مبنية):**
18. **دليل الزلفي على الخريطة** — `/map`، موقع لكل بائع، زر اتجاهات/اتصال، صفحة `/dashboard/location`
19. **ملصق QR للمحل** — `/dashboard/qr` قابل للطباعة (SVG على السيرفر) + تتبع `?src=qr`
20. **سفراء الأحياء** — تقديم طلب، مراجعة إدارية، بادج علني بصفحة الحي، مكافأة شرائح إعلان
21. **نشرة "وش الجديد" الأسبوعية بالإيميل** — اشتراك بلا حساب، إلغاء اشتراك عام، cron أسبوعي
22. **إغلاق حلقة "رشّح مشروعًا"** — مطابقة تلقائية عند انضمام المحل المرشَّح بنفسه، رابط واتساب مباشر للإدارة

23. **الدليل العام للزلفي** (PLAN.md §23 — حل مشكلة "البيضة والدجاجة") — دليل محلات معروفة حتى
    لو أصحابها ما سجّلوا، مصدره أدمن فقط من مصادر عامة (خرائط قوقل)، ظاهر بصفحة `/map` بقسم منفصل،
    مع زر "هذا محلي — اطلب التحكم" (Claim) يفتح طلب مراجعة إدارية.
    `migrations/…60`, `app/admin/directory`, `app/map/claim-button.tsx`

**الحالة النهائية لأفكار PLAN.md المعقّدة**: 26 مبنية، 0 ناقصة (عدا عزل الدِيَر — مؤجّل بقرار، راجع
IDEAS.md §"مؤجّلة")، 5 مؤجّلة خارج نطاقي بقرار موثّق (أضيفت شبكة التوصيل/الـEscrow §22 — تحتاج
ترخيص ساما وتنظيم قانوني، خارج نطاق ما أقدر أسويه بدون مراجعتك). التفاصيل الكاملة بـ[IDEAS.md](IDEAS.md).

---

## ⏭️ المتبقي — أولوية منخفضة، مو حرجة

- [ ] `NEXT_PUBLIC_SITE_URL` فاضي بـ`.env.local` محليًا (مضبوط بـVercel، محلي فقط)
- [ ] تقليل `as any` (213 موضع) — ولّد أنواع Supabase (`supabase gen types typescript`)
- [ ] تقسيم الملفات الضخمة (`moderation-client.tsx` 607 سطر، `seller/[slug]/page.tsx` 555 سطر)
- [ ] بعد أول بائعين حقيقيين: اختبار كل تدفقات الكتابة الفعلية (وليس فقط الفتح) — 21 ميزة معقّدة
      مبنية بالكود لكن ما زالت غير مُختبرة ببيانات حقيقية (راجع IDEAS.md الجدول الأول)

---

## ملاحظات تشغيلية

- **لا تحفظ أي شي على قرص C** — استخدم D فقط (القرص C أقل من 3GB فاضي).
- الرفع التلقائي لـGitHub بعد كل تعديل ذو معنى (CLAUDE.md) — بدون انتظار تأكيد.
- محرر Supabase SQL ينفّذ اللصقة كاملة كمعاملة واحدة: أي خطأ = تراجع تلك اللصقة فقط. قسّم اللصقات الكبيرة.
- `npm run build` و`npm run dev` ما يشتغلون بنفس الوقت (يفسدون `.next`).
- كل الصفحات الجديدة مكتوبة تتحمّل غياب هجرة لم تُطبَّق بعد (ترجع فارغة، لا تكسر) — تحقق من هذا
  النمط (`lib/data/sellers.ts` مثال) قبل إضافة صفحة جديدة تعتمد على عمود/دالة من هجرة لم تُطبَّق.
